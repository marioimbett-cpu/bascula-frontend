# En modo demo no necesitas configurar nada — las rutas /api/* del propio proyecto
# (carpeta src/app/api/) sirven datos simulados. Descomenta y ajusta esta línea
# solo cuando conectes un backend real:
# NEXT_PUBLIC_API_URL=https://tu-backend.com/api
